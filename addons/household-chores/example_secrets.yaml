# Example Home Assistant secrets.yaml entry for the chores add-on endpoint
# Add this to your Home Assistant secrets.yaml file and adjust the IP/port

chores_action_endpoint: !lambda |
  method: post
  url: "http://homeassistant.local:8000/ha/action"
  content_type: "application/json"
  payload: '{"action": "{{ action }}"}'
